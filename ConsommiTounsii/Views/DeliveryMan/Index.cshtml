@model IEnumerable<ConsommiTounsii.Models.DeliveryMan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.firstname_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lastname_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateofbirth_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.address_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email_user)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.start_time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.end_time)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.firstname_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lastname_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateofbirth_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.address_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email_user)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.start_time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.end_time)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.user_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.user_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.user_id })
        </td>
    </tr>
}

</table>
